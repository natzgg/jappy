import InfoCard from "@/components/info-card";
import { Bookmark } from "lucide-react";

const Shortlist = () => {
  return <InfoCard label="Shortlist" count={32} Icon={Bookmark} />;
};

export default Shortlist;
