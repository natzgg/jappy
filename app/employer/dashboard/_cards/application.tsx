import InfoCard from "@/components/info-card";
import { Mails } from "lucide-react";

const Application = () => {
  return <InfoCard label="Application" count={200} Icon={Mails} />;
};

export default Application;
